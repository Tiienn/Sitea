# Model attribution

- Player avatar ("Site visitor") — character.glb plus its 13 motion-capture
  clips (idle, walk, run, jump, walkback, strafes, turns) are Adobe Mixamo
  exports hosted on the project's Supabase assets bucket, used under the
  Adobe Mixamo license (registered in src/constants/avatars.js).
- The avatar registry may also host CC0 GLBs dropped into this folder
  later; attribute them here when added.
